The karatsuba algorithm, however instead of dividing by two, the chosen number is divided in three instances. 
The math operations are made just using Strings, only in the last digit they are resolved.       
